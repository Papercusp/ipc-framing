{
  "name": "@papercusp/ipc-framing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Length-prefixed binary frame codec for IPC/socket streams: [4B BE length][1B type][payload], with a streaming decoder. Pure, zero dependencies, zero domain coupling.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
